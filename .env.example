NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only — NEVER prefix with NEXT_PUBLIC_, never commit the real value.
# The new-style `sb_secret_...` secret key (Dashboard → Settings → API Keys →
# Secret keys), the replacement for the legacy service_role JWT. Bypasses RLS
# and all function grants. Currently used only by the username-login lookup
# (src/lib/supabase/service.ts) — see decisions.md D9.
SUPABASE_SECRET_KEY=
