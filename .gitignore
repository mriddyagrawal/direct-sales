node_modules/
.next/
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
.vercel/
coverage/
supabase/.temp/
next-env.d.ts

# Tally agent local config — holds the stock-push secret; never commit it.
tally-agent/agent_config.ini

# Tally export artifacts — real shop names, balances and raw replies. Never commit.
tally-agent/*.csv
tally-agent/**/*.csv
tally-agent/GanpatiSync/
tally-agent/GanpatiCredit/
tally-agent/GanpatiLedger/
tally-agent/raw_*.xml
tally-agent/probe_*.xml
tally-agent/**/*.xml
tally-agent/NewOutput/
tally-agent/ledger_state.txt
