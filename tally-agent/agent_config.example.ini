# --- Auto-submit config for the Tally stock agent ---------------------------
# Copy this file to  agent_config.ini  (same folder), then paste in push_secret.
# With agent_config.ini present, running the export ALSO submits the stock to the
# app in one click. Without it, the script just writes the CSV (upload manually).
#
# supabase_url and anon_key are PUBLIC (they already ship inside the web app).
# push_secret is the ONLY real credential, and it can do exactly one thing:
# update stock counts. It can't read orders, change prices, or anything else.
# Still, treat it like a password - don't share it. (agent_config.ini is
# git-ignored, so it is never committed.)

[app]
supabase_url = https://ugjwcbxyyuowiyhczcrh.supabase.co
anon_key     = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVnandjYnh5eXVvd2l5aGN6Y3JoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODMzNDkxMDgsImV4cCI6MjA5ODkyNTEwOH0.P8NcV89cSpR-4BmNsD7gGMbHbz-HMxdAmEeM98cJFR0
push_secret  = PASTE_THE_STOCK_PUSH_SECRET_HERE
